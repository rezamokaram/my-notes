# grub2

/boot/efi/EFI/fedora/grub.cfg

this is the configuration file of grub.

grub2-mkconfig -o /boot/grub/grub.cfg -> generate the grub2 cfg in the path (ubuntu path)

update-grub -> updates the grub by cfg

install-grub -> installs the by cfg
